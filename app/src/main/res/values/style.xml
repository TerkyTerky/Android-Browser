<resources>
    <!-- Base application theme.  Theme.AppCompat.Light.DarkActionBar -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="buttonSmall">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">25dp</item>
        <item name="android:minWidth">25dp</item>
        <item name="android:background">@drawable/bg_txt_button</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="buttonSmallNoMargin" parent="@style/buttonSmall">
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:layout_marginBottom">0dp</item>
    </style>

    <style name="buttonMainUIButton" parent="@style/buttonSmallNoMargin">
        <item name="android:onClick">onMainUiClick</item>
    </style>

</resources>
